#![allow(unused)]
use std::io::prelude::*;
use std::fs::File;

pub fn write_to_file(content:&[u8], filename: String ) -> std::io::Result<()> {
    Ok(())
//      let mut file = File::create(filename)?;
//         // Write a slice of bytes to the file
//         file.write_all(content);
//             //&[1,1,0,0,0,0,104,101,108,108,111])?;
    

// Ok(())
}

